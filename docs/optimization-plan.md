# OPT 推理优化施工点

当前范围：O0–O6。定位止于公开 SDK 调用、参数、耗时与返回结果。
2026-09-24 冷启动后恢复验收：O3 两种二维缓存小图通过，OPT/Qwen 短回归通过。
O4 整图输出和 KV 全零，释放阶段触及原 180 秒总时限。
随后最小恢复检查、三组输入/单层定位通过；加入 embedding/head 的单层前缀图
在 VerifyGraph 超过 120 秒预算后被终止，现有新的保护标记，板卡测试暂停。
O5 完整模型和 O6 性能对照依赖 O4 数值通过，暂未运行。

| 编号 | 工作 | 验收与依赖 |
|---|---|---|
| O0 | 冷启动后先 selection、再 32-token 全模型基线；记录 boot、SDK/binary hash、等待、CPU token、TTFT 与 tokens/s | 权限恢复后运行；数值正确与延迟异常分开报告 |
| O1 | 阶段/轮次心跳、独立 selection 入口、SDK 调用计时；分开初始化、prefill、首预测、decode、释放及各层组件 | 不新增张量读回；计时开关对照；API 次数匹配实际 consumed/predicted token；纯主机单测 |
| O2 | 冷启动后长等待仍复现时，缩减 selection 调用序列；与三类既有内存诊断统一说明，各自独立执行 | 未复现则保留证据与诊断入口，不宣告根因修复、不新增猜测性的测试矩阵 |
| O3 | 小图验证产生 K/V、原位追加和 Attention 读取的图内依赖 | 无完整 KV 复制/双缓冲；检查是否避免逐 token 重新验证图，验证历史保持与 reset |
| O4 | 完整 decode 合为一个 SDK 图；算子构造和图执行解耦，保留必要输出、按需开放诊断输出 | 依赖 O3；每 decode 步一次图执行；CPU/KV、初始化、内存与吞吐对照 |
| O5 | 固定小块 prefill，验证因果 mask、连续 KV 写入及尾块，再测 32-token 输入 | 每块一次整图；检查 prefill/decode 权重和 KV 共享；验收首 token、后续 decode 和 TTFT |
| O6 | 固定相同输入输出，对照 capacity 64/128；收益明确再考虑分桶/有效前缀 | 不用全量复制、逐步重编译或不断增多的图数量换取收益 |

执行：O0 → O1；O2 按观测触发。之后 O3 → O4 → O5；O6 依计时证据决定。
单图目标指一次完整 decode 或一个 prefill 块；自回归循环仍由主机调度。

O0–O2 的运行命令、日志字段及证据保存在
[SDK 计时与等待诊断](../tests/np101-sdk-timing.md)。三类内存复现沿用
[内存诊断](../tests/np101-memory.md)，不重复实现扫描器或分配框架。

2026-09-23 冷启动基线：O0 数值与生命周期通过，提交线程未复现原先约
30.7 秒的等待。32-token decode 为 0.3695/0.3728 tokens/s，TTFT 中位数
82.350/82.135 秒，低吞吐问题仍在。O2 的进一步缩减条件当前未触发；
现有 selection 支持独立运行和逐调用记录，三个内存诊断入口继续保留。

O1 已通过同配置开关对照：关闭/开启汇总计时分别为 0.3789/0.3812 tokens/s，
TTFT 中位数为 81.003/80.349 秒。两组 CPU token、KV/传输次数与正常释放
通过；计时组的公开 API 次数也匹配实际 token 数。该小样本未见明显计时开销。
汇总组图执行占请求时间 70.91%，KV 图重新验证占 13.56%，标量上传占
9.38%，KV 拷贝图执行占 5.99%；这些时间彼此不重复计入。SDK 内部耗时
不作进一步推断。

当前交付：O0 完成；O1 完成；O2 的独立入口与证据整理完成，进一步缩减
须等待长等待再次复现。后续状态如下；本轮不关闭三类内存问题或执行引擎/物理驻留证明。

| 施工点 | 已交付 | 恢复后的验收 |
|---|---|---|
| O3 | 二维单列和四列/单列共享缓存小图通过；历史、因果读取、尾块、reset、正常释放、无逐步 reverify 均通过 | 保留三维失败记录；小图通过不扩大为整模型尺度的支持声明 |
| O4 | 24 层整图可建图、可执行；5/3/5 请求均输出全零 token/KV，数值失败；进入释放后被总时限终止 | 恢复后先缩小验证权重绑定、打包控制输入和 embedding，再测单层；修正数值后再测整图及释放 |
| O5 | 四 token 固定块，逐 query 因果 mask，尾部复用单 token 图，prefill/decode 共用权重与 KV | 9/5/9-token A/B/A；CPU 前缀与重复 A 精确一致，再进入自然语义 32-token 输入 |
| O6 | 同一 CPU 参考、32-token 输入、最多 4 个输出的 capacity 64/128 独立进程对照入口 | 检查相同输入输出/SDK/build/boot；汇总 tokens/s、TTFT、初始化和 RSS；单次无预热结果只作初步观察 |

离线实现已通过全量编译、104 项 Python 测试和 4 项 CTest。2026-09-24
又完成 OPT 249 项、Qwen 353 项短回归及两个缓存小图。整图数值仍失败，
不能报告有效 tokens/s；完整 prefill、capacity 64/128 对照继续阻塞。

整图日志：SetupGraph 33.72 秒，VerifyGraph 40.64 秒，初始化约 95 秒；
释放超过 36 秒后被原总时限打断。这次 SIGTERM 来自测试器，不是新的 SIGSEGV。
现已改为首个 token 对照失败后跳过剩余请求并正常释放；总预算改为 decode
360 秒、双图 600 秒，旧 CPU 参考包复用时也采用新预算。改动只通过离线验证，
仍须验证正常释放。不得通过延长等待掩盖数值错误或绕过恢复保护。

具体命令、逐项结果和获授权读取的系统日志摘要见
[整图实验验收](../tests/np101-graph-optimization.md)。系统日志中的 PAT 提示也出现
于通过的测试；本次未见 NP101 内核崩溃记录，尚不能确定全零结果的根因。

同一次启动中的最小恢复检查完成 7 组输入、23 项数值/生命周期检查，耗时
0.77 秒；图释放合计 0.033 秒、context 释放 0.053 秒，未见秒级驱动等待。
证据和旧标记保存在 `.cache/runs/recovery-probe-20260924/`。当前无需据此要求
再次冷启动；完整图数值和释放验收仍未通过，不直接重跑完整图。

失败整图的原始 decode 间隔折算为 0.4733 次/秒，旧版正确基线为
0.3812 tokens/s。前者输出全零，且输入长度、capacity 和预热条件不同，
不能将算术差值约 24% 报告为有效加速。仅确认每步一次图执行、无逐 token
reverify 的调用结构已实现；修正数值后仍须同配置比较。

后续排查：合成词表查询 38 项、真实词表两次查询 13 项、真实第 0 层两步
联合计算 30 项全部通过，单层 KV 正常；全零根因仍未定位。单层前缀图包含
embedding/head，capacity 为 64，不能与原 capacity 16 整图作单变量比较。
它在 VerifyGraph 中持续使用 CPU，未进入 RunGraph；超过 120 秒总预算后
进程退出，新的恢复保护保留。下一轮先缩小为合成词表的 lookup/head 多消费者
组合，再统一 capacity 16 逐段接入。具体命令、逐项证据与头文件选项见
[图内数据路径排查](../tests/np101-graph-pipeline.md)。

2026-09-24 当前约束：const 与非 const 的应用张量载荷分别限制为 1 GiB，
共享引用只计一份，wrapper 临时分配计入峰值，超限在 SDK 分配前拒绝。
图诊断增加建图前、Setup 后、Verify 后及运行后的全部共享权重逐字节读回；
任何不一致先阻止数值验收。SDK 内部 workspace/副本未知，该限制不能恢复旧驱动
的地址布局，也不能避开历史坏数据位置。按用户最新指示，本轮仅编译与离线测试；
保留恢复标记，等待冷启动及用户开始测试的指令，再按链接中的顺序验证。

随后用户确认冷启动，启动 ID 已变化，设备权限为 0666。分段限额下首个合成
lookup 的 const/非 const 载荷峰值仅 76/1020 字节；全部 592 字节共享权重在
Setup 前、Setup 后、Verify 后读回一致。VerifyGraph 耗时 22.372 秒返回成功，
首次 RunGraph 未返回即到达原 30 秒总时限。一次获授权的内核日志读取确认
11:00:51 和 11:01:21 两次 NP101 hang/自动恢复，后一次在进程退出后；不能据
SDK 成功状态或进程退出认定设备健康。新的恢复保护保留，其余板卡测试未启动。
lookup 默认总预算已改为 120 秒供恢复后使用，但本轮不以延长时限重试已报告
hang 的设备。两步、capacity 16 的 24 层 CPU 参考已准备。详细时间线及复现
入口见 [图内数据路径排查](../tests/np101-graph-pipeline.md)。
